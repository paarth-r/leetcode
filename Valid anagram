class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        if len(s) != len(t):
            return False
        for n in range(len(s)):
            if s.count(s[n]) == t.count(s[n]):
                pass
            else:
                return False
        return True
